---
name: CI (latest)

on:
  push:
    branches:
      - 'feature/**'
    tags:
      - 'v*.*.*'

env:
  GHACU_VERSION: 2.0.2

jobs:
  unit:
    name: Unit Testing
    timeout-minutes: 5
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2.3.1
        with:
          ref: ${{ github.ref }}
      - name: Setup .NET Core SDK
        uses: actions/setup-dotnet@v1.5.0
        with:
          dotnet-version: 5.0.100-preview.6.20318.15
      - run: dotnet test
  functional:
    timeout-minutes: 10
    name: Functional Testing
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]    
      fail-fast: false
    runs-on: ${{ matrix.os }}
    steps:
      - name: Setup .NET Core SDK
        uses: actions/setup-dotnet@v1.5.0
        with:
          dotnet-version: 5.0.100-preview.6.20318.15
      - name: Install application (Windows)
        if: ${{ matrix.os == 'windows-latest' }}
        run: |
          Invoke-WebRequest -Uri https://github.com/fabasoad/ghacu/releases/download/v$env:GHACU_VERSION/ghacu-$env:GHACU_VERSION-win-x64.tgz -OutFile ghacu-$env:GHACU_VERSION-win-x64.tgz
          tar -xvf ghacu-$env:GHACU_VERSION-win-x64.tgz
          $env:Path += ";${{ github.workspace }}\ghacu-$env:GHACU_VERSION-win-x64"
          Write-Output "::set-env name=PATH::$env:Path"
        shell: pwsh
      - name: Install application (Linux)
        if: ${{ matrix.os == 'ubuntu-latest' }}
        run: |
          cd ~ && wget https://github.com/fabasoad/ghacu/releases/download/v$GHACU_VERSION/ghacu-$GHACU_VERSION-linux-x64.tgz
          tar -xvf ghacu-$GHACU_VERSION-linux-x64.tgz
          chmod +x ~/ghacu-$GHACU_VERSION-linux-x64/ghacu
          PATH=$PATH:~/ghacu-$GHACU_VERSION-linux-x64
          echo "::set-env name=PATH::$PATH"
      - name: Install application (MacOS)
        if: ${{ matrix.os == 'macos-latest' }}
        run: |
          cd ~ && wget https://github.com/fabasoad/ghacu/releases/download/v$GHACU_VERSION/ghacu-$GHACU_VERSION-osx-x64.tgz
          tar -xvf ghacu-$GHACU_VERSION-osx-x64.tgz
          chmod +x ~/ghacu-$GHACU_VERSION-osx-x64/ghacu
          PATH=$PATH:~/ghacu-$GHACU_VERSION-osx-x64
          echo "::set-env name=PATH::$PATH"
      - name: Run GHACU
        run: ghacu --version
